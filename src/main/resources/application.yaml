server:
  port: ${APP_PORT:8080}

spring:
  main:
    allow-bean-definition-overriding: true
  data:
    mongodb:
#      uri: mongodb+srv://gsantosc18:bancoMongo%40123@cluster0.vnkd4.mongodb.net/myFirstDatabase?retryWrites=true&w=majority
      host: mongodb
      port: 27017
      database: desafio
      username: admin
      password: password
      authentication-database: admin
  rabbitmq:
    username: rabbit
    password: rabbit
    port: 5672
    host: rabbit

feign:
  autoconfiguration:
    jackson:
      enabled: true
user:
  url: "http://user-info.herokuapp.com"

rabbit:
  exchange: sicredi
  queues:
    voto: voto
    resultado: resultado
  routingkeys:
    voto: votacao
    resultado: resultado